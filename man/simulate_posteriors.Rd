% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_posteriors.R
\name{simulate_posteriors}
\alias{simulate_posteriors}
\title{Simulate Bayesian posterior distributions across experimental sample sizes}
\usage{
simulate_posteriors(
  N,
  sigma_B,
  sims = 5000,
  tau = 0.1,
  sigma_Y = 1,
  sigma_g = 0.2,
  g = 0,
  beta = 0
)
}
\arguments{
\item{N}{Integer. Experimental sample size.}

\item{sigma_B}{Numeric. Standard deviation of the sampling-bias distribution.
Set to 0 for no bias uncertainty.}

\item{sims}{Integer. Number of simulated experimental results. Default 5000.}

\item{tau}{Numeric. True PATE. Default 0.1.}

\item{sigma_Y}{Numeric. Outcome standard deviation. Default 1.}

\item{sigma_g}{Numeric. Prior SD on the PATE. Default 0.2.}

\item{g}{Numeric. Prior mean on the PATE. Default 0.}

\item{beta}{Numeric. Prior mean on sampling bias. Default 0.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{N}, \code{sigma_B}, and \code{posterior_mean}.
}
\description{
Draws \code{sims} experimental results from a normal data-generating process with
true PATE \code{tau} and possible sampling bias, computes the posterior mean for
each draw using \code{\link[=posterior_mean_fn]{posterior_mean_fn()}}, and returns a tidy tibble. Used in
Chapter 11 to illustrate how sampling-bias uncertainty limits learning even
as sample size grows.
}
