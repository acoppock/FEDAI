% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double_lasso.R
\name{lm_double_lasso}
\alias{lm_double_lasso}
\title{Lin estimator with automatic LASSO covariate selection}
\usage{
lm_double_lasso(
  formula,
  covariates,
  data,
  weights = NULL,
  subset = NULL,
  clusters = NULL,
  se_type = NULL,
  ci = TRUE,
  alpha = 0.05,
  return_vcov = TRUE,
  try_cholesky = FALSE,
  lasso_args = list()
)
}
\arguments{
\item{formula}{Two-sided formula \code{Y ~ D}.}

\item{covariates}{Candidate covariates: a one-sided formula (\code{~ X1 + X2}),
a bare expression (\code{X1 + X2}), a character vector of column names, or a
variable holding any of the above.}

\item{data}{A data frame.}

\item{weights}{Optional weights vector.}

\item{subset}{Optional subset expression.}

\item{clusters}{Optional clusters vector for cluster-robust SEs.}

\item{se_type}{Standard-error type passed to \code{\link[estimatr:lm_lin]{estimatr::lm_lin()}}.}

\item{ci}{Logical; compute confidence intervals (default \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default \code{0.05}).}

\item{return_vcov}{Logical; return variance-covariance matrix (default
\code{TRUE}).}

\item{try_cholesky}{Logical; try Cholesky decomposition for speed (default
\code{FALSE}).}

\item{lasso_args}{Named list of additional arguments passed to
\code{\link[=lasso_select_covariates]{lasso_select_covariates()}} (e.g. \code{list(seed = 42)}).}
}
\value{
An \code{\link[estimatr:lm_lin]{estimatr::lm_lin()}} or \code{\link[estimatr:lm_robust]{estimatr::lm_robust()}} fit object.
Use \code{\link[broom:reexports]{broom::tidy()}} to extract results.
}
\description{
Wraps \code{\link[estimatr:lm_lin]{estimatr::lm_lin()}} with automatic covariate selection via
post-double-selection LASSO (\code{\link[=lasso_select_covariates]{lasso_select_covariates()}}). The \code{covariates}
argument specifies the \emph{candidate pool}; the LASSO trims it before passing
the selected subset to \code{lm_lin}. Falls back to \code{\link[estimatr:lm_robust]{estimatr::lm_robust()}} when
no covariates are selected (equivalent to a difference-in-means estimator).
}
\details{
The function signature is identical to \code{\link[estimatr:lm_lin]{estimatr::lm_lin()}} so it can be
used as a drop-in replacement anywhere \code{lm_lin} would appear, including
inside \code{\link[DeclareDesign:declare_estimator]{DeclareDesign::declare_estimator()}}.
}
\examples{
set.seed(1)
n <- 200
x <- matrix(rnorm(n * 20), n, 20)
colnames(x) <- paste0("x", 1:20)
d <- rbinom(n, 1, 0.5)
y <- 0.5 * d + x[, 1] * 0.8 + rnorm(n)
dat <- data.frame(y = y, d = d, x)
lm_double_lasso(y ~ d, covariates = ~ x1 + x2 + x3, data = dat)

}
\references{
Belloni, A., Chernozhukov, V., & Hansen, C. (2014). Inference on
treatment effects after selection among high-dimensional controls.
\emph{Review of Economic Studies}, 81(2), 608--650.
}
