% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double_lasso.R
\name{lasso_select_covariates}
\alias{lasso_select_covariates}
\title{Post-double-selection LASSO covariate selector (Lin-aware)}
\usage{
lasso_select_covariates(formula, covariates, data, seed = 999)
}
\arguments{
\item{formula}{Two-sided formula \code{Y ~ D}.}

\item{covariates}{Candidate covariates: a one-sided formula (\code{~ X1 + X2}),
a bare expression (\code{X1 + X2}), a character vector of column names, or a
variable holding any of the above.}

\item{data}{A data frame.}

\item{seed}{Integer seed for reproducibility (default 999).}
}
\value{
A one-sided formula of selected column names, or \code{~1} when no
covariates are selected.
}
\description{
Selects covariates for use with \code{\link[estimatr:lm_lin]{estimatr::lm_lin()}} via double-selection
LASSO (Belloni, Chernozhukov & Hansen 2014). Lin-aware: runs LASSO of the
outcome on covariates separately in each treatment arm so that covariates
useful for either main effects or treatment interactions are captured.
Double-selection: also regresses each arm indicator on the covariates so
that confounders predictive of assignment are included. Regularisation uses
the one-SE rule (\code{lambda.1se}) via \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.
}
\references{
Belloni, A., Chernozhukov, V., & Hansen, C. (2014). Inference on
treatment effects after selection among high-dimensional controls.
\emph{Review of Economic Studies}, 81(2), 608--650.
}
